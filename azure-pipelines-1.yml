# Test commit 1

# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Application Build
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Docker@2
      displayName: Build an image
      inputs:
        command: build
        dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
        repository:  'shafiyaheena/pythondemo'
        tags: 'latest'
    - task: Docker@2
      inputs:
        containerRegistry: 'DockerHub'
        repository: 'shafiyaheena/pythondemo'
        tags: 'latest'
        command: 'push'
        
    - bash: echo "This is test stage"
      displayName: Test stage

- stage: Deliver
  displayName: ContinousDeploy
  jobs:
  - job: DevDeploy
    displayName: Deploy to Dev Env
    pool:
      vmImage: ubuntu-latest
    steps:
    - bash: echo "Deploying to Development Environment"
      displayName: Dev Deploy
- stage: QATDeploy
  displayName: ContinousDeploy
  jobs:
  - job: QATDeploy
    dependsOn: DevDeploy 
    condition: succeeded('DevDeploy')
    displayName: Deploy to QAT Env
    pool:
      vmImage: ubuntu-latest
    steps:
    - bash: echo "Deploying to QAT Environment"
      displayName: QAT Deploy
- stage: StagingDeploy
  displayName: ContinousDeploy
  jobs:
  - job: StagingDeploy
    dependsOn: QATDeploy
    condition: succeeded('QATDeploy')
    displayName: Deploy to Staging Env
    pool:
      vmImage: ubuntu-latest
    steps:
    - bash: echo "Deploying to Staging Environment"
      displayName: Staging Deploy
- stage: ProductionDeploy
  displayName: ContinousDeploy
  jobs:
  - job: ProductionDeploy
    dependsOn: StagingDeploy
    condition: succeeded('StagingDeploy')
    displayName: Deploy to Production Env
    pool:
      vmImage: ubuntu-latest
    steps:
    - bash: echo "Deploying to Production Environment"
      displayName: Production Deploy
